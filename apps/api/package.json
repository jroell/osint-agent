{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "lint": "bunx tsc --noEmit",
    "build": "bun build src/index.ts --target=bun --outdir=dist"
  },
  "dependencies": {
    "postgres": "^3.4.4",
    "elysia": "^1.1.0",
    "@elysiajs/cors": "^1.1.0",
    "pino": "^9.5.0",
    "pino-pretty": "^11.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
